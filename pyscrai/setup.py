from setuptools import setup, find_packages

setup(
    name="pyscrai",
    version="0.1.0",
    description="PyScrAI module for Universalis project.",
    author="Your Name",
    packages=find_packages(),
    install_requires=[],  # Add dependencies here if needed
    include_package_data=True,
)